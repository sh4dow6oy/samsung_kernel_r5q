name: Build sh4dow6oy Kernel - R5Q (OpenELA Final Fix - Linker Conflict)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout Samsung Source
        uses: actions/checkout@v4
        with:
          repository: Roynas-Android-Playground/kernel_samsung_r3q
          ref: master

      - name: Install Build Tools
        run: |
          sudo apt-get update
          sudo apt-get install -y bc build-essential flex bison lz4 libssl-dev libelf-dev \
          ccache python3 python-is-python3 libarchive-tools wget zstd libtinfo6 zip \
          gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi device-tree-compiler rsync

      - name: Setup Neutron Clang
        run: |
          mkdir -p clang
          wget -q https://github.com/Neutron-Toolchains/clang-build-catalogue/releases/download/10032024/neutron-clang-10032024.tar.zst
          tar -I zstd -xf neutron-clang-10032024.tar.zst -C clang

      - name: Inject OpenELA & Conflict Resolution
        run: |
          wget -q https://github.com/openela/kernel-lts/archive/refs/tags/v4.14.356-openela-rc1.tar.gz -O openela.tar.gz
          mkdir openela_src
          tar -xzf openela.tar.gz -C openela_src --strip-components=1
          
          # Injectăm OpenELA, dar fără a suprascrie fișierele deja existente
          rsync -av --ignore-existing openela_src/ .
          
          # 1. KernelSU & SCSI Fix
          sed -i '/kernelsu/d' drivers/android/Kconfig
          find . -type f -name "*.c" -exec sed -i 's/SCSI_IOCTL_GET_IDLUN/0x5382/g' {} +
          
          # 2. FIX MULTIPLE DEFINITIONS (Nuclear Option)
          # Comentăm guvernatorul bw_hwmon direct în Makefile-ul devfreq pentru a opri duplicarea
          sed -i 's/obj-$(CONFIG_DEVFREQ_GOV_BW_HWMON)/# obj-$(CONFIG_DEVFREQ_GOV_BW_HWMON)/g' drivers/devfreq/Makefile
          
          # 3. FIX TARGET CORE (Dezactivăm SCSI Target complet)
          sed -i 's/obj-$(CONFIG_TARGET_CORE)/# obj-$(CONFIG_TARGET_CORE)/g' drivers/target/Makefile
          
          # 4. Samsung Stub & DTS Restore
          echo 'void dma_contiguous_early_fixup(void *res_cma) { }' > drivers/base/samsung_stub.c
          [ -f drivers/base/Makefile ] && echo 'obj-y += samsung_stub.o' >> drivers/base/Makefile
          git checkout HEAD -- arch/arm64/boot/dts
          
          # 5. Fix pentru fs/compat_ioctl.c (Eroarea de la mărimea array-ului)
          sed -i 's/sort(ioctl_pointer, ARRAY_SIZE(ioctl_pointer)/ \/\/ sort(ioctl_pointer, 0/g' fs/compat_ioctl.c

      - name: Build Kernel
        run: |
          export PATH="$GITHUB_WORKSPACE/clang/bin:$PATH"
          MAKE_ARGS="ARCH=arm64 LLVM=1 LLVM_IAS=1 CC=clang LD=ld.lld CROSS_COMPILE=aarch64-linux-gnu- CROSS_COMPILE_ARM32=arm-linux-gnueabi- O=out"
          
          make $MAKE_ARGS r5q_defconfig
          
          # Dezactivăm modulele care provoacă conflicte de simboluri la linkare
          ./scripts/config --file out/.config \
            -e CONFIG_SAMSUNG_PRODUCT_SHIP \
            -e CONFIG_BUILD_ARM64_DTB_IMAGE \
            -d CONFIG_LTO -d CONFIG_LTO_CLANG -e CONFIG_LTO_NONE \
            -d CONFIG_WERROR \
            -d CONFIG_DEVFREQ_GOV_BW_HWMON \
            -d CONFIG_TARGET_CORE
            
          make $MAKE_ARGS olddefconfig
          # Folosim -fcommon pentru a permite definiții multiple dacă mai apar (dar le-am blocat manual pe cele mari)
          make -j$(nproc --all) $MAKE_ARGS KCFLAGS="-Wno-error -fno-common -Wno-implicit-function-declaration"
          
          make $MAKE_ARGS dtbs

      - name: Manual Hardware Assembly
        run: |
          DTB_FILE=$(find out/arch/arm64/boot/dts -name "sm8150-r5q*.dtb" | head -n 1)
          if [ -n "$DTB_FILE" ]; then
            cat out/arch/arm64/boot/Image.gz "$DTB_FILE" > out/arch/arm64/boot/Image.gz-dtb
          fi
          MKDTBOIMG=$(find . -name "mkdtboimg.py" | head -n 1)
          DTBOS=$(find out/arch/arm64/boot/dts -name "*.dtbo")
          [ -n "$DTBOS" ] && [ -n "$MKDTBOIMG" ] && python3 $MKDTBOIMG create out/dtbo.img $DTBOS

      - name: AnyKernel3 Packaging
        run: |
          git clone https://github.com/osm0sis/AnyKernel3.git
          [ -f out/arch/arm64/boot/Image.gz-dtb ] && cp out/arch/arm64/boot/Image.gz-dtb AnyKernel3/Image.gz || cp out/arch/arm64/boot/Image.gz AnyKernel3/Image.gz
          [ -f out/dtbo.img ] && cp out/dtbo.img AnyKernel3/dtbo.img
          sed -i 's/device.name1=maguro/device.name1=r5q/g' AnyKernel3/anykernel.sh
          sed -i 's/do.devicecheck=1/do.devicecheck=0/g' AnyKernel3/anykernel.sh
          cd AnyKernel3 && zip -r9 ../sh4dow6oy-OpenELA-R5Q-Success.zip *

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Kernel-R5Q-OpenELA
          path: sh4dow6oy-OpenELA-R5Q-Success.zip
