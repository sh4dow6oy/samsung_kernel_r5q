name: Build Roynas R5Q - OneUI 7 (Android 15) Ready
on:
  workflow_dispatch:

env:
  SUBLEVEL: "356" # Forțăm kernel-ul la 4.14.356 pentru compatibilitate Android 15
  AUTHOR: "sh4dow6oy"
  DEVICE: "r5q"
  DEFCONFIG: "r5q_defconfig"

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4
        with:
          repository: Roynas-Android-Playground/kernel_samsung_r3q
          ref: master
          fetch-depth: 1

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y bc build-essential flex bison lz4 libssl-dev libelf-dev \
          ccache python3 python-is-python3 libarchive-tools wget zstd libtinfo6 zip

      - name: Setup Neutron Clang
        run: |
          mkdir clang
          wget -q https://github.com/Neutron-Toolchains/clang-build-catalogue/releases/download/10032024/neutron-clang-10032024.tar.zst
          tar -I zstd -xf neutron-clang-10032024.tar.zst -C clang

      - name: Patch for OneUI 7 & Android 15
        run: |
          echo "Aplicăm fix-urile pentru versiune, BPF și Partiții Dinamice..."

          # 1. FORȚARE SUBLEVEL (Să nu mai apară .190 în dmesg)
          sed -i "s/^SUBLEVEL =.*/SUBLEVEL = ${{ env.SUBLEVEL }}/" Makefile
          
          # 2. BPF UAPI (JLT, JLE + NUMA Fix)
          if ! grep -q "BPF_JLT" include/uapi/linux/bpf.h; then
            sed -i '/BPF_JSET/a \	BPF_JLT    = 0xa0, \/* LT */ \n	BPF_JLE    = 0xb0, \/* LE */ \n	BPF_JSLT   = 0xc0, \/* SLT */ \n	BPF_JSLE   = 0xd0, \/* SLE */' include/uapi/linux/bpf.h
          fi
          sed -i '/BPF_F_RDONLY/a #define BPF_F_NUMA_NODE (1U << 2)' include/uapi/linux/bpf.h

          # 3. BPF CORE & VERIFIER
          sed -i 's/bool bpf_dump_raw_ok(void)/bool bpf_dump_raw_ok(void) { return true; } \/\/ /' kernel/bpf/core.c || true
          sed -i '/check_helper_call(struct bpf_verifier_env/a \	if (env->prog->type == BPF_PROG_TYPE_UNSPEC) return -EINVAL;' kernel/bpf/verifier.c || true

          # --- PATCH SELINUX GENFSCON (PENTRU GSI/ONEUI 7) ---
          # Această modificare face kernel-ul să ignore erorile de context din vendor_sepolicy.cil
          # permițând boot-ul chiar dacă regulile de securitate sunt vechi.
          
          sed -i 's/if (IS_ERR(newc)) {/if (0) { \/\/ Ignorăm erorile genfscon/g' security/selinux/ss/services.c || true
          
          # --- FIX PENTRU EBPF SYSCALL ---
          # Forțăm sistemul să permită apelurile BPF chiar dacă SELinux este activ
          sed -i 's/return avc_has_perm(sid, target_sid, SECCLASS_BPF, bpf_perm, NULL);/return 0;/g' security/selinux/hooks.c || true
          
          # 4. NUCLEAR KERNELSU DISABLE (Evităm conflictele de Binder)
          sed -i '/kernelsu/d' drivers/android/Kconfig || true
          sed -i '/kernelsu/d' drivers/android/Makefile || true
          rm -rf drivers/kernelsu

          # 5. vDSO Fix (Pentru Linker-ul Neutron)
          find arch/arm64/kernel/vdso -name "Makefile*" -exec sed -i 's/-Wl,-n//g' {} +
          find arch/arm64/kernel/vdso -name "Makefile*" -exec sed -i 's/-Wl,-T//g' {} +

      - name: Build Kernel
        run: |
          export PATH="$GITHUB_WORKSPACE/clang/bin:$PATH"
          sed -i "s/^EXTRAVERSION =.*/EXTRAVERSION = -${{ env.AUTHOR }}-OneUI7/" Makefile
          
          MAKE_ARGS="ARCH=arm64 LLVM=1 LLVM_IAS=1 CC=clang LD=ld.lld AR=llvm-ar NM=llvm-nm OBJCOPY=llvm-objcopy OBJDUMP=llvm-objdump STRIP=llvm-strip CROSS_COMPILE=aarch64-linux-gnu- CROSS_COMPILE_ARM32=arm-linux-gnueabi- O=out"

          # Configurare cu suport EROFS și Partiții Dinamice pentru Android 15
          make $MAKE_ARGS ${{ env.DEFCONFIG }}
          
          ./scripts/config --file out/.config \
            -e CONFIG_BPF -e CONFIG_BPF_SYSCALL -e CONFIG_BPF_JIT -e CONFIG_BPF_JIT_ALWAYS_ON \
            -e CONFIG_NET_CLS_BPF -e CONFIG_CGROUP_BPF \
            -e CONFIG_EROFS_FS -e CONFIG_EROFS_FS_ZIP -e CONFIG_EROFS_FS_CLUSTER_PAGE_LIMIT \
            -e CONFIG_ANDROID_BINDERFS -e CONFIG_ANDROID_BINDER_IPC \
            -e CONFIG_OVERLAY_FS \
            -d CONFIG_KSU -d CONFIG_ASSIST_KSU \
            -d CONFIG_LTO -d CONFIG_LTO_CLANG -e CONFIG_LTO_NONE
          
          make $MAKE_ARGS olddefconfig

          # Compilare finală
          make -j$(nproc --all) $MAKE_ARGS KCFLAGS="-fcommon -Wno-error -Wno-unused-variable -Wno-unused-function -Wno-implicit-function-declaration"

      - name: Package for Recovery
        run: |
          IMAGE="$GITHUB_WORKSPACE/out/arch/arm64/boot/Image.gz-dtb"
          if [ ! -f "$IMAGE" ]; then IMAGE="$GITHUB_WORKSPACE/out/arch/arm64/boot/Image.gz"; fi
          git clone https://github.com/osm0sis/AnyKernel3.git
          cp $IMAGE AnyKernel3/Image.gz
          # Setăm numele device-ului în scriptul de instalare
          sed -i 's/device.name1=maguro/device.name1=${{ env.DEVICE }}/g' AnyKernel3/anykernel.sh
          cd AnyKernel3 && zip -r9 ../Kernel-OneUI7-Ready.zip *

      - name: Upload Kernel
        uses: actions/upload-artifact@v4
        with:
          name: Kernel-OneUI7-R5Q
          path: "*.zip"
