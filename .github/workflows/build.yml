name: S10 Lite - Lineage Style Fix v73

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout S10 Lite Source
        uses: actions/checkout@v4
        with:
          path: s10-lite-src

      - name: Setup Toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y bc bison flex libssl-dev libelf-dev dwarves make python3 lz4 perl git
          git clone --depth=1 https://gitlab.com/clangsantoni/zyc_clang.git toolchain/zyc-clang

      - name: Prepare Base & Upstream 4.14.356
        run: |
          git clone --depth=1 -b lineage-23.0 https://github.com/LineageOS/android_kernel_google_msm-4.14.git kernel-res
          cd kernel-res
          git remote add stable https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux-stable.git
          git fetch stable linux-4.14.y
          git merge FETCH_HEAD --no-edit || echo "Merge handled"

      - name: Inject Drivers
        run: |
          cp -r s10-lite-src/drivers/soc/samsung kernel-res/drivers/soc/ || true
          cp -r s10-lite-src/drivers/camera kernel-res/drivers/ || true
          cp -r s10-lite-src/drivers/fingerprint kernel-res/drivers/ || true
          cp -r s10-lite-src/drivers/sensors kernel-res/drivers/ || true
          cp -r s10-lite-src/arch/arm64/boot/dts/samsung kernel-res/arch/arm64/boot/dts/ || true
          cp s10-lite-src/arch/arm64/configs/r5q_eur_open_defconfig kernel-res/arch/arm64/configs/

      - name: Surgical Fixes v73 (LineageOS Method)
        run: |
          cd kernel-res
          
          # 1. SDIO LINEAGE FIX
          # Resetăm fișierul la starea originală pentru a curăța experimentele noastre
          git checkout drivers/mmc/core/sdio.c
          
          # Metoda Lineage: 
          # Orice label 'out:' trebuie să fie urmat de o instrucțiune, chiar dacă e goală (;).
          # De asemenea, ne asigurăm că label-ul există unde e chemat.
          sed -i 's/out:$/out: ;/g' drivers/mmc/core/sdio.c
          
          # Adăugăm label-ul 'out: ;' la finalul funcțiilor unde Samsung l-a uitat/șters
          # dar unde codul încă face 'goto out;'
          # Aceasta este o abordare sigură: inserăm 'out: ;' chiar înainte de ultima acoladă a funcțiilor.
          sed -i '/^}$/i out: ;' drivers/mmc/core/sdio.c
          
          # Curățăm duplicatele: dacă avem două 'out: ;' consecutive, păstrăm doar una.
          sed -i 'N;/^\(out: ;\)\n\1$/D;P;D' drivers/mmc/core/sdio.c

          # 2. FIX DM-BOW & SDHCI
          sed -i '15i #undef SECTOR_SIZE' drivers/md/dm-bow.c
          sed -i 's/sdhci_cmd_irq(host, intmask & SDHCI_INT_CMD_MASK, \&intmask)/sdhci_cmd_irq(host, intmask \& SDHCI_INT_CMD_MASK)/g' drivers/mmc/host/sdhci.c
          sed -i '/intmask_p/d' drivers/mmc/host/sdhci.c

          # 3. TRACING PATH FIX
          sed -i 's|#define TRACE_INCLUDE_PATH .|#define TRACE_INCLUDE_PATH ../../drivers/hid|g' drivers/hid/hid-trace.h

          # 4. WALT FIXES
          sed -i '40i static long s10_dummy_val;\nstatic long s10_dummy_val_scaled;' kernel/sched/walt.c
          sed -i 's/rq->cum_window_demand/s10_dummy_val/g' kernel/sched/walt.c
          sed -i 's/rq->cumulative_runnable_avg/s10_dummy_val/g' kernel/sched/walt.c

      - name: Build Kernel
        run: |
          cd kernel-res
          export PATH=$(pwd)/../toolchain/zyc-clang/bin:$PATH
          export CLANG_TRIPLE=aarch64-linux-gnu-
          export CROSS_COMPILE=aarch64-linux-gnu-
          export CROSS_COMPILE_ARM32=arm-linux-gnueabi-
          
          MAKE_ARGS="ARCH=arm64 CC=clang HOSTCC=gcc LLVM=1 LLVM_IAS=1"
          # Folosim -Wno-unused-label ca să nu ne bată la cap dacă un label e acolo dar nu e folosit
          export KCFLAGS="-Wno-error -Wno-unused-label -Wno-implicit-function-declaration -Wno-incompatible-pointer-types -I$(pwd)/drivers/hid"

          make $MAKE_ARGS r5q_eur_open_defconfig
          ./scripts/config --disable CONFIG_CC_STACKPROTECTOR_STRONG
          ./scripts/config --enable CONFIG_CC_STACKPROTECTOR_NONE
          
          make $MAKE_ARGS olddefconfig
          make -j$(nproc --all) $MAKE_ARGS Image.gz-dtb

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: kernel-S10Lite-4.14.356-v73
          path: kernel-res/arch/arm64/boot/Image.gz-dtb
