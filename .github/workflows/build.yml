name: Build R5Q - Native LineageOS 21 (EUR Open)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout LineageOS 21 Kernel (Base)
        uses: actions/checkout@v4
        with:
          repository: LineageOS/android_kernel_google_msm-4.14
          ref: lineage-21
          path: kernel

      - name: Checkout Mainey R5Q Source (For Hardware Drivers)
        uses: actions/checkout@v4
        with:
          repository: mainey/android_kernel_samsung_r5q
          ref: lineage-19.0
          path: samsung

      - name: Install Build Tools
        run: |
          sudo apt-get update
          sudo apt-get install -y bc build-essential flex bison lz4 libssl-dev libelf-dev \
          ccache python3 python-is-python3 libarchive-tools wget zstd libtinfo6 zip \
          gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi device-tree-compiler rsync git

      - name: Setup Toolchain (Neutron Clang)
        run: |
          mkdir -p clang
          wget -q https://github.com/Neutron-Toolchains/clang-build-catalogue/releases/download/10032024/neutron-clang-10032024.tar.zst
          tar -I zstd -xf neutron-clang-10032024.tar.zst -C clang

      - name: Inject Samsung R5Q EUR Hardware
        run: |
          # 1. Copiem Device Tree-ul Samsung (Esențial pentru hardware-ul r5q)
          mkdir -p kernel/arch/arm64/boot/dts/samsung
          cp -r samsung/arch/arm64/boot/dts/samsung/* kernel/arch/arm64/boot/dts/samsung/
          
          # 2. Copiem driverele proprietare Samsung (Senzori, Baterie, Soc)
          rsync -a --ignore-existing samsung/drivers/soc/samsung/ kernel/drivers/soc/samsung/
          rsync -a --ignore-existing samsung/drivers/sensors/ kernel/drivers/sensors/
          rsync -a --ignore-existing samsung/drivers/battery_v2/ kernel/drivers/battery_v2/
          
          # 3. Copiem config-ul EUR Open specific
          cp samsung/arch/arm64/configs/r5q_eur_open_defconfig kernel/arch/arm64/configs/r5q_eur_open_defconfig
          
          # 4. Ajustăm Makefile-ul pentru a "păcăli" Android 15 (Kernel 5.4 Spoof)
          cd kernel
          sed -i 's/VERSION = 4/VERSION = 5/g' Makefile
          sed -i 's/PATCHLEVEL = 14/PATCHLEVEL = 4/g' Makefile

      - name: Build Kernel
        run: |
          cd kernel
          export PATH="$GITHUB_WORKSPACE/clang/bin:$PATH"
          MAKE_ARGS="ARCH=arm64 LLVM=1 LLVM_IAS=1 CC=clang LD=ld.lld CROSS_COMPILE=aarch64-linux-gnu- CROSS_COMPILE_ARM32=arm-linux-gnueabi- O=out"
          
          # Folosim config-ul EUR OPEN
          make $MAKE_ARGS r5q_eur_open_defconfig
          
          # Forțăm activarea eBPF și PSI pentru Android 15
          ./scripts/config --file out/.config \
            -e CONFIG_BPF -e CONFIG_BPF_SYSCALL -e CONFIG_BPF_JIT \
            -e CONFIG_PSI -e CONFIG_NETFILTER_XT_MATCH_BPF -e CONFIG_CGROUP_BPF \
            -d CONFIG_WERROR
            
          make $MAKE_ARGS olddefconfig
          
          # Compilăm cu flag-uri de toleranță pentru mix-ul de surse
          make -j$(nproc --all) $MAKE_ARGS KCFLAGS="-Wno-error -fcommon -Wno-implicit-function-declaration"
          make $MAKE_ARGS dtbs

      - name: Package
        run: |
          cd kernel
          # Căutăm DTB-ul generat pentru EUR model
          DTB_FILE=$(find out/arch/arm64/boot/dts/samsung -name "*r5q*eur*.dtb" | head -n 1)
          [ -z "$DTB_FILE" ] && DTB_FILE=$(find out/arch/arm64/boot/dts/samsung -name "sm8150-r5q*.dtb" | head -n 1)
          
          cat out/arch/arm64/boot/Image.gz "$DTB_FILE" > out/arch/arm64/boot/Image.gz-dtb
          
          git clone https://github.com/osm0sis/AnyKernel3.git
          cp out/arch/arm64/boot/Image.gz-dtb AnyKernel3/Image.gz
          
          sed -i 's/device.name1=maguro/device.name1=r5q/g' AnyKernel3/anykernel.sh
          cd AnyKernel3 && zip -r9 ../R5Q-A15-EUR-LINEAGE-BASE.zip *

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: Kernel-R5Q-EUR-Native
          path: R5Q-A15-EUR-LINEAGE-BASE.zip
