name: Build Samsung SM8150 Kernel

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Kernel Source
      uses: actions/checkout@v4
      with:
        repository: LumiPlayground/android_kernel_samsung_sm8150
        ref: auriana
        path: kernel

    - name: Install Build Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y bc build-essential flex bison libssl-dev libelf-dev clang lld llvm libncurses-dev wget git

    - name: Setup AOSP Clang (r383902)
      run: |
        mkdir -p clang
        wget https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/3857008389202edac32d57008bb8c99d2c957f9d/clang-r383902.tar.gz
        tar -xf clang-r383902.tar.gz -C clang
        # Salvăm calea absolută
        echo "CLANG_PATH=$(pwd)/clang/bin" >> $GITHUB_ENV

    - name: Download AOSP GCC 4.9
      run: |
        git clone --depth=1 https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9 gcc64
        git clone --depth=1 https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/arm/arm-linux-androideabi-4.9 gcc32
        # Salvăm căile absolute către binare (inclusiv prefixul)
        echo "GCC64_PREFIX=$(pwd)/gcc64/bin/aarch64-linux-android-" >> $GITHUB_ENV
        echo "GCC32_PREFIX=$(pwd)/gcc32/bin/arm-linux-androideabi-" >> $GITHUB_ENV
        echo "TOOLCHAIN_PATH=$(pwd)/gcc64/bin:$(pwd)/gcc32/bin" >> $GITHUB_ENV

    - name: Build Kernel
      run: |
        cd kernel
        
        # Setează PATH-ul să includă toate uneltele
        export PATH="${{ env.CLANG_PATH }}:${{ env.TOOLCHAIN_PATH }}:$PATH"
        
        export ARCH=arm64
        export SUBARCH=arm64
        
        # --- FIXURI SURSĂ ---
        # 1. Bypass Clang Safety Switch
        sed -i 's/-ftrivial-auto-var-init=zero/-ftrivial-auto-var-init=zero -enable-trivial-auto-var-init-zero-knowing-it-will-be-removed-from-clang/g' Makefile
        

        # Configurare
        cat arch/arm64/configs/sm8150_sec_defconfig arch/arm64/configs/sm8150_sec_r5q_eur_open.config > arch/arm64/configs/combined_defconfig
        
        # Injectăm manual opțiunile de securitate pentru a evita check-ul de compiler
        echo "CONFIG_CC_STACKPROTECTOR_NONE=y" >> arch/arm64/configs/combined_defconfig
        echo "# CONFIG_CC_STACKPROTECTOR_STRONG is not set" >> arch/arm64/configs/combined_defconfig

        # Variabilele de Build cu CĂI ABSOLUTE
        MAKE_VARS="O=out \
                   ARCH=arm64 \
                   CC=clang \
                   HOSTCC=gcc \
                   CLANG_TRIPLE=aarch64-linux-gnu- \
                   CROSS_COMPILE=${{ env.GCC64_PREFIX }} \
                   CROSS_COMPILE_ARM32=${{ env.GCC32_PREFIX }} \
                   LD=ld.lld \
                   DTC_EXT=$(pwd)/scripts/dtc/dtc \
                   KCFLAGS='-w -fno-stack-protector -enable-trivial-auto-var-init-zero-knowing-it-will-be-removed-from-clang'"

        # Execuție configurare
        make $MAKE_VARS combined_defconfig
        make $MAKE_VARS olddefconfig

        # Compilare propriu-zisă
        make -j$(nproc --all) $MAKE_VARS
        
    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Kernel-SM8150-r5q
        path: |
          kernel/out/arch/arm64/boot/Image.gz-dtb
          kernel/out/arch/arm64/boot/dtbo.img
          kernel/out/.config
