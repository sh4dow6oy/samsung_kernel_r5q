name: Build Lumi R5Q - Final Fix
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4
        with:
          repository: LumiPlayground/android_kernel_samsung_sm8150
          ref: rawr-bpf
          path: kernel
          fetch-depth: 1

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y bc build-essential flex bison lz4 libssl-dev libelf-dev \
          ccache python3 python-is-python3 libarchive-tools wget zstd zip \
          gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi device-tree-compiler clang

      - name: Setup Toolchain (Neutron Clang)
        run: |
          mkdir -p clang
          wget -q https://github.com/Ylarod/setup-ndk-clang/releases/download/prebuilt/clang-linux-x86-ndk-r29-r563880c.tar.zst
          tar -I zstd -xf clang-linux-x86-ndk-r29-r563880c.tar.zst -C clang

      - name: Build Kernel
        run: |
          cd kernel
          chmod -R +x tools/
          export PATH="$GITHUB_WORKSPACE/clang/bin:$PATH"
          
          # Fix Multiple Definition
          grep -r "bool main_ext_ss_not_working" . | cut -d: -f1 | xargs sed -i 's/bool main_ext_ss_not_working/static bool main_ext_ss_not_working/g' || true
          
          export MAKE_ARGS="ARCH=arm64 LLVM=1 LLVM_IAS=1 CC=clang \
            DTC_EXT=$(pwd)/tools/dtc \
            CONFIG_BUILD_ARM64_DT_OVERLAY=y \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
            LD=ld.lld \
            O=out"

          make $MAKE_ARGS r5q_eur_open_defconfig
          
          # BPF & Security Fixes
          ./scripts/config --file out/.config \
            -e CONFIG_BPF_SYSCALL -e CONFIG_BPF_JIT -e CONFIG_CGROUP_BPF \
            -e CONFIG_NETFILTER_XT_MATCH_BPF -d CONFIG_RTIC \
            -e CONFIG_BUILD_ARM64_DT_OVERLAY -e CONFIG_SEC_R5Q_PROJECT \
            -e CONFIG_PROJECT_R5Q -e CONFIG_SAMSUNG_PRODUCT_SHIP
            
          make $MAKE_ARGS olddefconfig
          make -j$(nproc --all) $MAKE_ARGS KCFLAGS="-fcommon -w" O=out Image.gz-dtb dtbs

      - name: Professional DTBO Packaging (Fixed Paths)
        run: |
          # 1. Download mkdtboimg.py (Link stabil)
          wget -q https://raw.githubusercontent.com/LineageOS/android_system_libufdt/lineage-18.1/utils/src/mkdtboimg.py -O mkdtboimg.py
          chmod +x mkdtboimg.py

          # 2. Setup AnyKernel3
          git clone https://github.com/osm0sis/AnyKernel3.git
          cp kernel/out/arch/arm64/boot/Image.gz-dtb AnyKernel3/Image.gz
          
          # 3. Detectare automata a folderului Renovation
          # Uneori structura este arch/arm64/boot/dts/samsung/renovation
          # Alteori este direct arch/arm64/boot/dts/renovation
          RENO_PATH=$(find kernel/arch/arm64/boot/dts -name "renovation" -type d | head -n 1)
          DTS_ROOT="kernel/arch/arm64/boot/dts"
          
          echo "Folderul Renovation gasit la: $RENO_PATH"
          mkdir -p dtbo_manual

          # Lista de fisiere conform Makefile-ului tau
          FILES_TO_BUILD=(
            "sm8150-sec-r5q-overlay-r00.dts"
            "sm8150-sec-r5q-overlay-r01.dts"
            "sm8150-sec-r5q-overlay-r07.dts"
            "sm8150-sec-r5q-overlay-r08.dts"
            "sm8150-sec-r5q-overlay-r09.dts"
            "sm8150-sec-system-update-overlay.dts"
          )

          for dts_name in "${FILES_TO_BUILD[@]}"; do
            target_file=$(find "$RENO_PATH" -name "$dts_name")
            if [ -f "$target_file" ]; then
              echo "Compiling $(basename "$target_file")..."
              dtbo_out=$(basename "$dts_name" .dts).dtbo
              
              # Preprocesare Clang (Include toate dependintele .dtsi)
              clang -E -nostdinc -undef -D__DTS__ -x assembler-with-cpp \
                -I kernel/include \
                -I "$DTS_ROOT" \
                -I "$DTS_ROOT/qcom" \
                -I "$DTS_ROOT/samsung" \
                -I "$RENO_PATH" \
                "$target_file" -o "dtbo_manual/tmp.dts"
              
              # Compilare DTC binar
              kernel/tools/dtc -@ -I dts -O dtb -o "dtbo_manual/$dtbo_out" "dtbo_manual/tmp.dts"
            else
              echo "Warning: $dts_name NU a fost gasit in $RENO_PATH!"
            fi
          done

          # 4. Creare DTBO.img cu mkdtboimg (Tabela de indexare)
          if [ "$(ls -A dtbo_manual/*.dtbo 2>/dev/null)" ]; then
            python3 mkdtboimg.py create AnyKernel3/dtbo.img dtbo_manual/*.dtbo
            echo "DTBO.img a fost creat cu succes."
          else
            echo "EROARE: Nu s-a putut compila niciun .dtbo!"
            exit 1
          fi

          # 5. Configurare AnyKernel3 (Cai block S10 Lite si VBMeta Patch)
          cd AnyKernel3
          sed -i 's/do.devicecheck=1/do.devicecheck=0/g' anykernel.sh
          sed -i 's|block=auto;|block=/dev/block/by-name/boot;|g' anykernel.sh
          # Injectam linia dtbo_block dupa boot block
          sed -i '/block=\/dev\/block\/by-name\/boot;/a dtbo_block=/dev/block/by-name/dtbo;' anykernel.sh
          sed -i 's/patch_vbmeta_flag=auto/patch_vbmeta_flag=1/g' anykernel.sh
          
          zip -r9 ../Lumi-R5Q-Complete-BPF.zip *
