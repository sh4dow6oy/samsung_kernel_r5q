name: Build r5q Kernel - eBPF & Upstream CLO

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y bc bison build-essential flex gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi git libssl-dev python3 clang llvm lld

      - name: Clone Mainey Kernel
        run: |
          git clone --depth=500 https://github.com/mainey/android_kernel_samsung_r5q.git -b lineage-19.0 kernel
          cd kernel
          git config user.email "wizard@build.com"
          git config user.name "Kernel-Wizard"

      - name: Upstream from CodeLinaro (CLO)
        run: |
          cd kernel
          # Adăugăm CLO ca remote
          git remote add clo https://git.codelinaro.org/clo/la/kernel/msm.git
          # Fetch la ramura stabilă indicată de tine
          git fetch clo aosp-common/android-4.14-stable --depth=100
          
          echo "Încercăm merge-ul de upstream (CLO)..."
          # Folosim 'ours' ca strategie de rezolvare a conflictelor pentru a păstra driverele Samsung intacte
          git merge FETCH_HEAD -m "Upstream merge from CLO 4.14-stable" -X ours || echo "Merge-ul a generat conflicte, dar am păstrat baza Samsung."

      - name: Apply eBPF & Hardware Offload
        run: |
          cd kernel
          # Fetch eBPF base de la LineageOS
          git fetch https://github.com/LineageOS/android_kernel_samsung_sm7125 refs/changes/79/387879/1
          git checkout FETCH_HEAD -- include/linux/bpf.h \
                                     include/linux/bpf_verifier.h \
                                     include/linux/netdevice.h \
                                     include/uapi/linux/bpf.h \
                                     kernel/bpf/core.c \
                                     kernel/bpf/Makefile \
                                     kernel/bpf/offload.c \
                                     kernel/bpf/syscall.c \
                                     kernel/bpf/verifier.c \
                                     net/core/filter.c
          
          # Commit la fișierele eBPF forțate
          git add .
          git commit -m "Import eBPF core after upstream"

      - name: Patch Conflicts & Paths
        run: |
          cd kernel
          # Curățăm redefinirile cauzate de upstream
          sed -i '/struct bpf_line_info {/,/};/d' include/uapi/linux/bpf.h
          sed -i '/struct bpf_func_info {/,/};/d' include/uapi/linux/bpf.h
          
          # Fix BPF_OBJ_NAME_LEN & Samsung Debug
          sed -i '/#define _UAPI__LINUX_BPF_H/a #ifndef BPF_OBJ_NAME_LEN\n#define BPF_OBJ_NAME_LEN 16U\n#endif' include/uapi/linux/bpf.h
          sed -i 's/sec_debug_sched_msg/\/\/ sec_debug_sched_msg/g' arch/arm64/kernel/traps.c
          
          # Fix Toolchain Paths & Dummy secgetspf
          sed -i 's|$(srctree)/../toolchain/gcc/linux-x86/aarch64/aarch64-linux-android-4.9/bin/aarch64-linux-android-||g' Makefile
          sed -i 's|CROSS_COMPILE_ARM32 :=.*|CROSS_COMPILE_ARM32 := arm-linux-gnueabi-|g' Makefile
          echo -e "#!/bin/sh\necho '00000000000000000000000000000000'\nexit 0" > secgetspf
          chmod +x secgetspf

      - name: Configure and Build
        run: |
          cd kernel
          export ARCH=arm64
          export CROSS_COMPILE=aarch64-linux-gnu-
          export CROSS_COMPILE_ARM32=arm-linux-gnueabi-
          export CC=clang
          export CLANG_TRIPLE=aarch64-linux-gnu-
          export PATH=$PATH:$(pwd)

          mkdir -p out
          make O=out ARCH=arm64 r5q_eur_open_defconfig
          
          # Configurații eBPF & Connectivity
          scripts/config --file out/.config -e BPF -e BPF_SYSCALL -e BPF_JIT -e CGROUP_BPF
          scripts/config --file out/.config -e NETFILTER_XT_MATCH_BPF -e NET_ACT_BPF -e BPF_STREAM_PARSER
          scripts/config --file out/.config --set-val NR_CPUS 8
          scripts/config --file out/.config -d SEC_DEBUG
          
          yes "" | make O=out ARCH=arm64 oldconfig
          
          # Final Build
          make -j$(nproc --all) O=out ARCH=arm64 \
            LD=ld.lld \
            KCFLAGS="-Wno-error -Wno-implicit-function-declaration -fcommon -DNR_CPUS=8"

      - name: Upload Kernel Image
        uses: actions/upload-artifact@v4
        with:
          name: Kernel-r5q-Upstream-CLO
          path: |
            kernel/out/arch/arm64/boot/Image.gz-dtb
            kernel/out/arch/arm64/boot/dtbo.img
