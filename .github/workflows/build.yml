name: Build R5Q - Lumi BPF V43 (Stock Drivers Replacement)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout LumiPlayground Kernel (Baza ta)
        uses: actions/checkout@v4
        with:
          repository: LumiPlayground/android_kernel_samsung_sm8150
          ref: rawr-bpf
          path: kernel

      - name: Replace Drivers Folder with Stock Drivers & Fix Squeezer
        run: |
          cd kernel
          # 1. Ștergem folderul drivers actual pentru a nu lăsa reziduuri
          rm -rf drivers
          
          # 2. Descărcăm folderul drivers din kernelul stock mk7x7
          echo "Descărcăm folderul drivers din kernelul stock..."
          wget -q https://github.com/mk7x7/s10lite-stock-kernel/archive/refs/heads/main.zip -O stock.zip
          
          # 3. Extragem structura drivers direct în kernelul nostru
          unzip -q stock.zip 's10lite-stock-kernel-main/drivers/*'
          mv s10lite-stock-kernel-main/drivers .
          
          # 4. Curățăm fișierele temporare descărcate
          rm -rf s10lite-stock-kernel-main stock.zip
          
          # 5. FIX KERNELSU: Eliminăm liniile care caută KernelSU în fișierele stock
          if [ -f drivers/Kconfig ]; then
            sed -i '/kernelsu/d' drivers/Kconfig
          fi
          if [ -f drivers/Makefile ]; then
            sed -i '/kernelsu/d' drivers/Makefile
          fi
          
          # 6. FIX SAMSUNG SQUEEZER (ZRAM): Ștergem direct liniile problematice
          if [ -f drivers/block/zram/zram_drv.c ]; then
            echo "Eliminăm complet liniile Squeezer (SQZR) din driverul ZRAM..."
            sed -i '/SQZR_/d' drivers/block/zram/zram_drv.c
          fi
          
          echo "Folderul drivers a fost înlocuit, curățat de KernelSU și patch-uit pentru ZRAM!"
      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu binutils-aarch64-linux-gnu bc libssl-dev zstd zip python3 python-is-python3 libelf-dev

      - name: Setup Toolchain (Neutron Clang)
        run: |
          mkdir -p clang
          wget -q https://github.com/Neutron-Toolchains/clang-build-catalogue/releases/download/10032024/neutron-clang-10032024.tar.zst
          tar -I zstd -xf neutron-clang-10032024.tar.zst -C clang

      - name: Build Kernel
        run: |
          cd kernel
          export PATH="$GITHUB_WORKSPACE/clang/bin:$PATH"
          
          # 1. NUCLEAR CLEANUP (Prevenim Polly Errors pe noul folder drivers)
          find . -type f \( -name "Makefile*" -o -name "*.mk" \) -exec sed -i 's/-mllvm -polly[^ ]*//g' {} +
          find . -type f \( -name "Makefile*" -o -name "*.mk" \) -exec sed -i 's/-mllvm//g' {} +
          find . -type f \( -name "Makefile*" -o -name "*.mk" \) -exec sed -i 's/-polly[^ ]*//g' {} +

          # 2. FIX SPECIFIC VDSO (Prevenim eroarea /usr/bin/as)
          if [ -f arch/arm64/kernel/vdso/Makefile ]; then
            sed -i 's/-static//g' arch/arm64/kernel/vdso/Makefile
            sed -i 's/$(LDFLAGS)/$(filter-out -static,$(LDFLAGS))/g' arch/arm64/kernel/vdso/Makefile
            sed -i 's/$(CROSS_COMPILE)as/clang/g' arch/arm64/kernel/vdso/Makefile
          fi

          # 3. SETARE ARGUMENTE BUILD
          MAKE_ARGS='ARCH=arm64 
            LLVM=1 
            LLVM_IAS=1 
            CC=clang 
            LD=ld.lld 
            AR=llvm-ar 
            NM=llvm-nm 
            OBJCOPY=llvm-objcopy 
            OBJDUMP=llvm-objdump 
            STRIP=llvm-strip 
            AS=clang
            CROSS_COMPILE=aarch64-linux-gnu- 
            CROSS_COMPILE_ARM32=arm-linux-gnueabi- 
            CLANG_TRIPLE=aarch64-linux-gnu- 
            HOSTCC=clang 
            HOSTCXX=clang++ 
            HOSTCFLAGS=-Wno-error 
            O=out'

          make $MAKE_ARGS r5q_eur_open_defconfig

          # FORȚARE BPF JIT & CURĂȚARE SAMSUNG SECURITY
          ./scripts/config --file out/.config \
            -e CONFIG_BPF -e CONFIG_BPF_SYSCALL -e CONFIG_BPF_JIT -e CONFIG_BPF_JIT_ALWAYS_ON \
            -e CONFIG_HAVE_EBPF_JIT -e CONFIG_NET_ACT_BPF -e CONFIG_NET_CLS_BPF \
            -d CONFIG_SAMSUNG_RTIC -d CONFIG_SAMSUNG_RTIC_AMP \
            -d CONFIG_CC_STACKPROTECTOR_STRONG -e CONFIG_CC_STACKPROTECTOR_NONE \
            -d CONFIG_WERROR

          make $MAKE_ARGS olddefconfig

          # COMPILARE FINALĂ
          make -j$(nproc --all) $MAKE_ARGS Image.gz Image.gz-dtb dtbs \
            KCFLAGS="-w -fcommon --target=aarch64-linux-gnu" \
            KASFLAGS="--target=aarch64-linux-gnu" \
            LDFLAGS="--no-rosegment"

      - name: Assemble AnyKernel3
        run: |
          OUT="kernel/out/arch/arm64/boot"
          if [ -f "$OUT/Image.gz-dtb" ]; then
            IMAGE_FINAL="$OUT/Image.gz-dtb"
          else
            IMAGE_FINAL="$OUT/Image.gz"
          fi
          
          git clone https://github.com/osm0sis/AnyKernel3.git AK3
          rm -rf AK3/.git
          cp "$IMAGE_FINAL" AK3/Image.gz
          [ -f "$OUT/dtbo.img" ] && cp "$OUT/dtbo.img" AK3/dtbo.img
          
          cd AK3
          sed -i 's/device.name1=maguro/device.name1=r5q/g' anykernel.sh
          sed -i 's/device.name2=toroplus/device.name2=SM-G770F/g' anykernel.sh
          sed -i 's/do.devicecheck=1/do.devicecheck=0/g' anykernel.sh
          sed -i 's/header_version=0/header_version=2/g' anykernel.sh
          zip -r9 ../S10Lite_StockDrivers_BPF.zip *

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: S10Lite-StockDrivers
          path: S10Lite_StockDrivers_BPF.zip
