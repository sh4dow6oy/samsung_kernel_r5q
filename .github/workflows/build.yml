name: S10 Lite Kernel Build - v111

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout Source
        uses: actions/checkout@v4
        with:
          repository: mainey/android_kernel_samsung_r5q
          ref: lineage-18.1
          path: kernel-res
          fetch-depth: 0 # Avem nevoie de tot istoricul pentru merge

      - name: Setup Proton Clang
        run: |
          sudo apt-get update
          sudo apt-get install -y bc bison flex libssl-dev libelf-dev dwarves make python3 lz4 perl git curl
          git clone --depth=1 https://github.com/kdrag0n/proton-clang.git toolchain/proton-clang

      - name: Proper Upstream Merge
        run: |
          cd kernel-res
          git config user.email "ai@gemini.com"
          git config user.name "Gemini"
          git remote add stable https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux-stable.git
          # Fetch tag-uri pentru a putea face merge la v4.14.356
          git fetch stable --tags
          git merge v4.14.356 --no-edit || (git merge --abort && echo "Merge conflict major, continuam fara upstream momentan")

      - name: Dynamic Fixes (Camera & BPF)
        run: |
          cd kernel-res
          
          # Gasim fisierul de camera oriunde ar fi si initializam variabila 'i'
          CAM_FILE=$(find drivers/media -name "cam_vfe_bus_ver3.c" | head -n 1)
          if [ -f "$CAM_FILE" ]; then
            sed -i 's/uint32_t                     i;/uint32_t                     i = 0;/g' "$CAM_FILE"
            echo "Fixed camera file at: $CAM_FILE"
          fi

          # Fix SEC_DEBUG
          find arch/arm64 -name "sec_debug.h" -exec sed -i 's/extern void sec_debug_check_sum_interrupted/static inline void sec_debug_check_sum_interrupted/g' {} +
          echo "#define sec_debug_log_copy_line(x,...) do {} while (0)" >> include/linux/sec_debug.h

          # Injectare BPF pentru Android 15
          sed -i 's/CONFIG_BPF_SYSCALL=n/CONFIG_BPF_SYSCALL=y/' arch/arm64/configs/r5q_eur_open_defconfig
          {
            echo "CONFIG_BPF_JIT=y"
            echo "CONFIG_CGROUP_BPF=y"
            echo "CONFIG_NETFILTER_XT_MATCH_BPF=y"
          } >> arch/arm64/configs/r5q_eur_open_defconfig

      - name: Build Kernel
        run: |
          cd kernel-res
          export PATH=$(pwd)/../toolchain/proton-clang/bin:$PATH
          MAKE_ARGS="ARCH=arm64 CC=clang CROSS_COMPILE=aarch64-linux-gnu- CROSS_COMPILE_ARM32=arm-linux-gnueabi- LLVM=1 LLVM_IAS=1"
          export KCFLAGS="-Wno-everything -Wno-error -Idrivers/hid"

          make $MAKE_ARGS r5q_eur_open_defconfig
          make -j$(nproc --all) $MAKE_ARGS Image.gz-dtb || make -j$(nproc --all) $MAKE_ARGS Image.gz

      - name: Upload
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kernel-S10Lite-v111
          path: |
            kernel-res/arch/arm64/boot/Image.gz-dtb
            kernel-res/arch/arm64/boot/Image.gz
